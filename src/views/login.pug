extends base.pug 


block content 
    if errorMessage
        span=errorMessage

    form(method="POST")

        input(name="username", type="text", required,placeholder="사용자이름")
        input(name="password", type="password", required,placeholder="비밀번호")
        input(type="submit", value="로그인")
        br
        include partials/social-login
    hr        
    div.auth-switch
        span 계정이 없으신가요? 
        a(href="/join") 계정을 만들어보세요 &rarr; 
            
  
