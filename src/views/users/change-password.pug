extends ../base.pug 

block content
    if errorMessage
        span=errorMessage
    form(method="post")
        input(placeholder="이전비밀번호", type="password", name="oldPassword")
        input(placeholder="새로운비밀번호", type="password", name="newPassword")
        input(placeholder="비밀번호 확인", type="password",  name="passwordCheck")
        input(type="submit", value="변경")